export enum TOKEN_TYPE {
  ACCESS_TOKEN = 'access_token',
  REFRESH_TOKEN = 'refresh_token'
}

export enum USER_VERIFY_STATUS {
  UNVERIFIED = 'unverified',
  VERIFIED = 'verified',
  BANNED = 'banned'
}

export enum MEDIA_TYPE_QUERY {
  IMAGE = 'image',
  VIDEO = 'video',
  HLS = 'hls'
}

export enum PEOPLE_FOLLOW {
  Anyone = '0',
  Following = '1'
}
